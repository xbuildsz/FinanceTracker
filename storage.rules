rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Receipts: only the authenticated owner can read/write their folder
    match /receipts/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null
        && request.auth.uid == userId;
    }
  }
}
